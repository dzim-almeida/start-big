/**
 * Re-export do composable global de modal de clientes.
 * Mantido para compatibilidade com imports existentes.
 */
export {
  useCustomerModal,
  type ModalMode,
} from '@/shared/composables/modals/customers/useCustomerModal';
