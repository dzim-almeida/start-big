# Arquivo listando as dependencias do BACKEND-FASTAPI

pip install "fastapi[standard]"

pip install uvicorn "pydantic[email]" pytest httpx

pip install sqlalchemy

pip install "pydantic-settings"

pip3 install bcrypt

pip install python-jose[cryptography]
